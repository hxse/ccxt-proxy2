[project]
name = "ccxt-proxy2"
version = "1.0.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "ccxt",
    "pydantic",
    "pandas>=2.3.2",
    "pyarrow>=21.0.0",
]

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest",
]
