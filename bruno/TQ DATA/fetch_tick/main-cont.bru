meta {
  name: main-cont
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/tq/fetch_tick?symbol={{tqTickSymbol}}&data_length={{tqDataLength}}
  body: none
  auth: inherit
}

params:query {
  symbol: {{tqTickSymbol}}
  data_length: {{tqDataLength}}
  ~adj_type:
}

assert {
  res.status: eq 200
}
