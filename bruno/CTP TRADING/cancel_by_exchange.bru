meta {
  name: cancel_by_exchange sandbox [STATEFUL]
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/ctp/cancel_order
  body: json
  auth: inherit
}

body:json {
  {
    "mode": "sandbox",
    "by": "exchange_order",
    "exchange_id": "SHFE",
    "instrument_id": "rb2610",
    "order_sys_id": "       12345"
  }
}

docs {
  按需手动执行。先替换合约、价格或原订单标识；不要在状态未知时直接重试。完整说明见 /docs 的 CTP TRADING。
}
