meta {
  name: binance [STATEFUL]
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/ccxt/cancel_order
  body: json
  auth: inherit
}

body:json {
  {
    "exchange_name": "binance",
    "market": "future",
    "mode": "sandbox",
    "symbol": "BTC/USDT:USDT",
    "id": "replace-me"
  }
}
