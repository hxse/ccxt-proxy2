meta {
  name: kraken [STATEFUL]
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/ccxt/cancel_order
  body: json
  auth: inherit
}

body:json {
  {
    "exchange_name": "kraken",
    "market": "future",
    "mode": "sandbox",
    "symbol": "BTC/USD:USD",
    "id": "replace-me"
  }
}
