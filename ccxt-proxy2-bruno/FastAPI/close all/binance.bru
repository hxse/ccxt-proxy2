meta {
  name: binance
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/ccxt/close_all_orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "exchange_name": "binance",
    "symbol": "BTC/USDT"
  }
}
